# Database (Neon in production). Leave unset to use the local PGLite fallback.
# DATABASE_URL=postgres://user:pass@host/db

# xAI Imagine — required for generating plates
# XAI_API_KEY=

# Better Auth — set in production
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=https://your-domain.example
